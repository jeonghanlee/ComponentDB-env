# -*- mode: Makefile;-*-

# .PHONY: es_install es_start es_status es_mapping es_mapping_clean es_all

.PHONY: install.pkgs

install.pkgs:
	$(SUDOBASH) "$(TOP)/docker/scripts/required_pkgs.sh $(CDB_SUPPORT_DIR)"
	$(QUIET)echo "\nPlease source $(TOP)/docker/scripts/.sourceme\n"



# es_start:
# 	$(QUIET)$(SUDO) systemctl start elasticsearch

# es_status:	
# 	$(QUIET)systemctl status -l elasticsearch | cat

# ## Create the ES indexes and set up their mapping
# es_mapping:
# 	$(QUIET)bash $(SITE_TEMPLATE_PATH)/mapping_definitions.bash

# ## Clean all ES indexes and their mapping
# es_mapping_clean:
# 	$(QUIET)bash $(SITE_TEMPLATE_PATH)/clean_mapping_definitions.bash


# es_all: es_install es_start es_start es_mapping
